{% extends 'base.html.twig' %}

{% block title %}Putain, merde! Une erreur!{% endblock %}

{% block body %}
    <!-- This example requires Tailwind CSS v2.0+ -->
    <div class="relative bg-white overflow-hidden">
        <div class=" mx-auto">
            <div class="relative z-10 bg-white w-2/3  pb-28">
                <main class="mt-10 mx-auto px-4 mt-20 px-8">
                    <div class="text-left">
                        <h1 class="text-4xl tracking-tight font-extrabold text-gray-900 text-6xl">
                            <span class="block xl:inline">Ca c'est carrément une erreur </span>
                            <span class="block text-red-600 xl:inline">{{ status_code }}!</span>
                        </h1>
                        <p class="mt-3 text-base text-gray-500 mx-0 text-2xl">
                            Même que chez les britiches on dirait un peu un {{ status_text }}
                        </p>
                        <div class="mt-5 justify-start">
                            <p>
                                Le message intéressant que peut-être il explique pourquoi on en est
                                là c'est celui là: <strong>{{ exception.message }}</strong>
                            </p>
                            <p>
                                Si ça a pas aidé, merci d'envoyer tout ça par e-mail à moi (si tu sais pas mon adresse
                                ben désolé, tu peux pas).
                                J'ai besoin du message juste là en dessus et de tout le truc la en dessous.
                                Merci, bisou 😘
                            </p>
                            <hr>
                            <p style="margin:30px;font-style: italic; font-family: Courier, monospace">{{ exception.traceAsString|nl2br }}</p>

                            <a href="{{ path('edi_converter') }}"
                               class="w-full flex items-center justify-center px-8 py-3 border border-transparent text-white  hover:bg-red-900  font-medium rounded-md bg-red-400 mt-12">Retour
                                au monde des bisounours</a>
                        </div>
                    </div>
                </main>
            </div>
        </div>
        <div class="absolute inset-y-0 right-0 w-1/3">
            <img class="h-56 w-full object-cover w-full h-full"
                 src="https://media.giphy.com/media/lQlBLrgathaG1f4obw/giphy.gif" alt="">
        </div>
    </div>




{% endblock %}