{% extends 'base.html.twig' %}

{% block title %}Convert Bill to EDI{% endblock %}

{% block body %}
    {{ form_start(form) }}


    <!-- This example requires Tailwind CSS v2.0+ -->
    <div class="relative bg-white overflow-hidden">
        <div class="max-w-7xl mx-auto">
            <div class="relative z-10 pb-8 bg-white max-w-2xl w-full pb-28">
                <main class="mt-10 mx-auto max-w-7xl px-4 mt-20 px-8">
                    <div class="text-left">
                        <h1 class="text-4xl tracking-tight font-extrabold text-gray-900 text-6xl">
                            <span class="block xl:inline">Salut les </span>
                            <span class="block text-red-600 xl:inline">libraires!</span>
                        </h1>
                        <p class="mt-3 text-base text-gray-500 mx-0 text-2xl">
                            Juste la dessous, faut mettre le fichier .xml généré avec votre logiciel et l'envoyer ⬇️
                        </p>
                        <div class="mt-5 justify-start">
                            <div class="rounded-md shadow">
                                {{ form_widget(form.brochure, { 'attr': {'class': 'w-full flex items-center justify-center px-8 py-3 border border-transparent text-base font-medium rounded-md text-red-700 bg-red-100 hover:bg-red-200'} }) }}
                            </div>
                            <div class="mt-3">
                                {{ form_row(form.submit, { 'attr': {'class': 'w-full flex items-center justify-center px-8 py-3 border border-transparent text-white  hover:bg-red-900  font-medium rounded-md bg-red-400 mt-12'} }) }}

                            </div>
                        </div>
                    </div>
                </main>
            </div>
        </div>
        <div class="absolute inset-y-0 right-0 w-1/3">
            <img class="h-56 w-full object-cover w-full h-full" src="https://source.unsplash.com/random/?books" alt="">
        </div>
    </div>
    {{ form_end(form) }}

    <div class="relative bg-white overflow-hidden mt-10">
        <div class="max-w-7xl mx-auto">
            <div class="relative z-10 pb-8 bg-white max-w-2xl w-full pb-28">
                <main class="mt-10 mx-auto max-w-7xl px-4 mt-20 px-8">
                    <h1 class="text-2xl tracking-tight font-extrabold text-gray-900 text-6xl">
                        <span class="block xl:inline">Pis là y a toutes les factures que la BCU a pas encore vu </span>
                    </h1>
                    <ul class="mt-3">
                        {% for key,bill in existing_bills %}
                            <li class="m-1">🧾 {{ key }} <a href="{{ path('remove_file',{'file':key}) }}"
                                                            onclick="return confirm('Supprimer?')"
                                                            class="text-sm text-gray-300">Supprimer</a></li>
                        {% else %}
                            Oh ben y en a pas !
                        {% endfor %}
                    </ul>
                </main>
            </div>
        </div>
    </div>

{% endblock %}
